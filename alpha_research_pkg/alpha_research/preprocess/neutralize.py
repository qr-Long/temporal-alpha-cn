# Placeholder: neutralization requires industry/mcap panel, not included by default.
# We keep this module so the framework can be extended later.
from __future__ import annotations
import pandas as pd

def neutralize_placeholder(factor: pd.DataFrame) -> pd.DataFrame:
    return factor
